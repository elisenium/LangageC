#include <stdio.h>
#include <stdlib.h>

#define SIZE 50

int main() {
    /* code */
    int n_entiers, entier;
    printf("Entrez le nombre de données voulus : \n");
    scanf("%d", n_entiers);

    for (int i = 0; i < n_entiers; ++i)
    {
        /* code */
    }

    return 0;
}